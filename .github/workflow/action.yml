name: 'First workflow'
description: 'A simple action that prints Hello World and Done - bye!'
inputs: {}
runs:
  using: "composite"
  steps:
  - name: First step
    run: echo "Hello World"
    shell: bash
  - name: Second step
    run: echo "Done - bye!"
    shell: bash
