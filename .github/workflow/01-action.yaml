name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
    - name: First step
      run: echo "This is the first step of the first job of the first workflow"
